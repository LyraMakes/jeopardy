<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Jeopardy | Game #{{gameID}}</title>

    <script>
      function boardClick(x, y) {
        // Redirect to the question page
        window.location.href = `/jeopardy/game/{{gameID}}/question/c${y}q${x}`;
      }
    </script>

    <style>
      body {
        background-color: #000066;
      }

      .board-background {
        background-color: #060CE9;
      }

      table#board {
        width: 70%;
        margin-left: auto;
        margin-right: auto;

        table-layout: fixed;
      }

      #board th,
      #board td {
        width: 20%;
        word-wrap: break-word;
      }

      table#board,
      #board th,
      #board td {
        border-collapse: collapse;
        border-spacing: 0;
        border-color: black;
        border-style: solid;
        border-width: 1px;

        height: 140px;
      }

      #board th {
        font-size: 32px;
      }

      #board td {
        font-size: 72px;
      }


      .board-header {
        color: white;
      }

      .board-value {
        color: yellow;
        font-family: "Ariel Bold", sans-serif;
        font-size: 32px;
        text-align: center;
      }

      table#score {
        width: 10%;
        margin-left: auto;
        margin-right: auto;
      }

      #scores tr {
        text-align: center;
        font-size: 2em;
        font-weight: bold;
        color: #FFFFFF;
      }
    </style>
  </head>
  <body>


    <div id="board-parent">
      <table>
        <tr>
          <td>
            <table id="scores" class="score-background">
              {{ scoreBoard }}
            </table>
          </td>
          <td>
            <table id="board" class="board-background">
              {{ gameBoard }}
            </table>
          </td>
        </tr>
      </table>
    </div>


  </body>
</html>